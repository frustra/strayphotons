if(WIN32)
    add_subdirectory(windows)
elseif(UNIX)
    add_subdirectory(linux)
elseif(APPLE)
    add_subdirectory(apple)
elseif(ANDROID)
    add_subdirectory(android)
else()
    message(FATAL_ERROR "Unknown FMOD platform")
endif()
