{
	"_name": <%= name + "_plate" %>,
	"renderable": <%= 'sensor-plate-' + type %>,
	"transform": {
		<% if (locals.rotate) { %>"rotate": <%= rotate %>,<% } %>
		"translate": <%= translate %>
	},
	"physics": {
		"model": <%= 'sensor-plate-' + type %>,
		"dynamic": false
	}
},
{
	"_name": <%= name %>,
	"renderable": "sensor",
	"transform": {
		"parent": <%= name + "_plate" %>,
		"translate": [0, 0, 0.015]
	},
	"lightsensor": {
		"translate": [0, 0, 0.1],
		"direction": [0, 0, 1],
		"triggers": <%= triggers %>
	}
}