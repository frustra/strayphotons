{
	"_name": <%= name %>,
	"renderable": "spotlight-01",
	"light": {
		<% if (locals.on != null) { %>"on": <%= on %>,<% } %>
		"tint": <%= tint %>,
		"intensity": <%= intensity %>,
		"spotAngle": <%= spotAngle %>,
		"bulb": <%= name + "-bulb" %>,
		"shadowMapSize": 11,
		"shadowMapClip": [0.1, 70]
	},
	<% if (locals.physics) { %>
	"physics": {
		"model": "spotlight-01",
		"dynamic": false
	},
	<% } %>
	"transform": {
		<% if (locals.rotate) { %>"rotate": <%= rotate %>,<% } %>
		"translate": <%= translate %>
	}
},
{
	"_name": <%= name + "-bulb" %>,
	"renderable": "box",
	"transform": {
		"parent": <%= name %>,
		"translate": [0, 0, -0.06],
		"scale": [0.1, 0.1, 0.03]
	}
}
