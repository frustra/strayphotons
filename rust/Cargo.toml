[package]
name = "sp-rust"
version = "0.1.0"
authors = ["xthexder"]

[profile.dev]
opt-level = 2

[dependencies]
cxx = "1.0"
reqwest = { version = "0.11.0", features = ["blocking"] }
url = "2.2.0"

[build-dependencies]
cxx-build = "1.0"

[lib]
crate-type = ["staticlib"]
